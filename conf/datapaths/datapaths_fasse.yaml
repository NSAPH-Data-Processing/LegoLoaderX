# basefolder is the root of the data tree (relative to cwd, or an absolute path
# for a shared deploy). All paths in the pipeline are fixed relative to it.
basefolder: data

dirs:
  input:
    lego: /n/dominici_nsaph_l3/Lab/lego
  covars: /n/dominici_lab/Lab/lego_loader_x/output
  health: /n/dominici_nsaph_l3/Lab/lego_loader_x/health
  embeddings: /n/dominici_lab/Lab/lego_loader_x/embeddings