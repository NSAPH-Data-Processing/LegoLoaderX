
defaults:
  - _self_
  - pipeline
  - var_group: gridmet

# this should be set directly by script
year: 2000
month: 1
day: 1
var: tmmx

spatial_res: zcta
temporal_res: daily

output_dir: data/output/

hydra:
  run:
    dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
